/MODULE.bazel.lock
/bazel-*
# CatchNip fork-only: CMake build directory.
# On macOS APFS/HFS+ (case-insensitive) the default `build/` collides with
# the top-level Bazel `BUILD` file, so downstream consumers use `_cmake_build*`.
/_cmake_build*/
